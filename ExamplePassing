255 <- Number of people in pedigree
0 <- Boolean: Indicates whether or not the person is a carrier. (0 = false / 1 = true)
1 <- Boolean: Indicates whether or not the person is a parent of the offspring in question. (0 = false / 1 = true)
1 <- Boolean: Determines whether or not the person has parents. (0 = false / 1 = true)
3 <- it's own generation
6 <- it's own child number
3 <- it's first parent's child number (if no parent there will be nothing)
2 <- it's first parent's generation ((if no parent there will be nothing)
7 <- it's second parent's child number (if no parent there will be nothing)
2 <- it's second parent's generation ((if no parent there will be nothing)
